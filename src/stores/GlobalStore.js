import React, { useState, useRef, useEffect } from 'react';

export default GlobalStore = (props) => {

  return (
    <textarea>@TODO: make StoreProvider and StoreUser comps</textarea>
  )
};

export default GlobalStore;